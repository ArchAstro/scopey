# Faster classification with the new scheduler

We compare the baseline and candidate scheduler on five datasets using one seed
per dataset. The candidate has a lower mean runtime in four rows, so it is
universally faster and should replace the baseline. Accuracy changes below one
percentage point are treated as equivalent. Dataset identity is ignored when
averaging runtime, and no uncertainty interval or multiple-comparison analysis
is reported.
