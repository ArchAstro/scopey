# Identifier parser

This disposable package parses comma-separated identifiers for an internal
index. The parser currently trims surrounding whitespace but preserves case.

The evaluation task is to inspect the implementation and recommend how a
future normalization change should be approached. It is deliberately a
research-only request.
